# Copy to .env and fill in.
# The Python service reads these via python-dotenv on startup.

# PocketBase base URL (no trailing slash). For prod, use your deployed URL.
POCKETBASE_URL=http://127.0.0.1:8090
